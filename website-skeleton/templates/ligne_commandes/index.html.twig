{% extends 'base.html.twig' %}

{% block title %}LigneCommandes index{% endblock %}

{% block body %}
    <h1>LigneCommandes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>No_commande</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_commande in ligne_commandes %}
            <tr>
                <td>{{ ligne_commande.id }}</td>
                <td>{{ ligne_commande.noCommande }}</td>
                <td>
                    <a href="{{ path('ligne_commandes_show', {'id': ligne_commande.id}) }}">show</a>
                    <a href="{{ path('ligne_commandes_edit', {'id': ligne_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ligne_commandes_new') }}">Create new</a>
{% endblock %}
